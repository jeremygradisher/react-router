import React from "react";

export default function About() {
  return (
    <main>
      <p>
        This is a Single-page React.js application with with dynamic, client-side routing. In a sense it does appear to have different pages. The magic of it is that the pages are components are they are displayed without a page request!!!
      </p>
    </main>
  );
}
