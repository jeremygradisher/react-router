import React from "react";

export default function About() {
  return (
    <main>
      <p>
        This is a Single-page application (SPA) built with React that has dynamic, client-side routing. In a sense it does appear to have different pages. The magic of it is that the pages are components and they are displayed without a http request which makes it lightning fast!!!
      </p>
      <p>
        Feel free to click around. This tutorial showed building the navigation and creating the routing with react-router-dom. There is dynamic page creation with authors and articles that have categories.
      </p>
    </main>
  );
}
